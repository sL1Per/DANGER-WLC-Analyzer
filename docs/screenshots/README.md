# README screenshots

PNGs referenced by the root `README.md` **## Screenshots** section.

Committed despite the repo-wide `*.png` ignore via the `!docs/screenshots/*.png`
exception in `.gitignore`. Keep them lean (~1400–2000px wide, PNG), cropped to
the app content (no browser chrome).

| File | View |
| --- | --- |
| `summary.png` | Summary tab — damage / healing done by source |
| `gear.png` | Gear tab — a boss pull with enchant/gem/socket problems flagged |
| `role-breakdown.png` | Role breakdown — a role sheet (Melee/Ranged) |
| `consumables.png` | Consumables tab — the per-player discipline matrix |
| `totem-twist.png` | The totem-twist air-slot timeline strip(s) |
